FROM openjdk

RUN mkdir -p /app/shoppingAPI

ENV APP_NAME="/target/rest-api-crud-shopping-fuzzy-1.0.00.jar"

COPY APP_NAME /app/shoppingAPI

EXPOSE 8080

ENTRYPOINT [ "java", "-jar", "/app/shoppingAPI/${APP_NAME}" ]
